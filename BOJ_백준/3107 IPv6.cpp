#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main() {

	string s;
	vector <bool, string> IPv6;
	IPv6.resize(8);
	cin >> s;
	int index = 0;
	for (int i = 0; i < s.size(); i++) {
		if()
	}


}